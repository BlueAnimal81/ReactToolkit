export var setPlayerCount = (value) => {
    return {
        type: 'SET_PLAYER_COUNT',
        value
    }
}

export var setResult = (value) => {
    return {
        type: 'SET_RESULT',
        value
    }
}